# 社内勤怠・有給管理アプリ 仕様書

| 項目 | 内容 |
|---|---|
| 文書バージョン | 0.4 |
| 作成日 | 2026-09-01 |
| 更新日 | 2026-09-01 |
| ステータス | 要件整理中 |
| 想定利用者数 | 約20名 |
| 運用環境 | XServerレンタルサーバー／ブラウザ・PWA・Electron |

## v0.3 → v0.4 の変更点

- 代休（休日出勤に応じて発生する休暇）を有給とは別枠のデータとして追加（§7.8, §6, §8, §10, §12.1）
- 管理者権限とは独立した「閲覧権限」の仕組みを追加。個人単位・グループ単位で付与でき、閲覧のみ可能。管理者は任意の組み合わせで付与でき、社員本人は自分のデータの公開先のみ設定できる（§4.3, §12.1, §14, §17）
- 管理者画面に、指定月の全社員出退勤打刻データをCSVエクスポートする機能を追加（§9.4, §10, §14, §17）
  - 文字コードはBOM付きUTF-8とする（暫定、要確認）
  - 初期版は打刻時刻のみを出力し、勤怠連絡（遅刻・早退等）は含めない（暫定、要確認）

---

## 1. 目的

現在GoogleフォームおよびGoogleスプレッドシートで運用している社内手続きを、ログイン式の社内Webアプリへ段階的に集約する。ブラウザ版を共通基盤とし、スマートフォンではPWA、PCではブラウザまたはElectron版から同じ機能とデータを利用できる構成とする。

主な目的は以下のとおり。

- 社員が自分の有給残数や申請履歴をいつでも確認できるようにする
- マネージャーや管理担当者への個別問い合わせを減らす
- 氏名選択による誤登録をなくす
- 有給の登録、取消、残数計算を一元管理する
- 出退勤および勤怠連絡を将来的にGoogleフォームから置き換える
- 操作履歴を保存し、変更経緯を確認できるようにする

## 2. システムの位置づけ

本システムは、社内の勤怠・有給関連機能をまとめる社内ポータルとする。

アプリケーションの本体はXServer上で稼働するWebアプリとする。iPhone・Androidではホーム画面へ追加したPWAとして利用できるようにし、Windows・macOSでは通常のブラウザに加えてElectron版を選択できるようにする。PWA版、Electron版ともに独立したデータを持たず、同一のサーバー、認証、データベースを利用する。

初期段階では有給管理を中心に実装し、出退勤は基本的な形のみを用意する。出退勤の詳細な集計・修正・締め処理などは後続フェーズで決定する。

## 3. 対象範囲

### 3.1 MVPで実装する機能

- メールアドレスとパスワードによるログイン
- パスワード再設定
- 社員アカウント管理
- 社員向けホーム画面
- 有給残数確認
- 有給予定登録
- 有給予定取消
- 有給取得履歴・取消履歴確認
- 管理者による有給付与・残数調整
- 管理者による社員別有給状況確認
- 遅刻、早退、欠勤などの勤怠連絡
- 概略的な出勤・退勤打刻
- 操作履歴保存
- スマートフォン対応
- PWAとしてのホーム画面追加とアプリ形式での起動
- ログイン状態の安全な保持

### 3.2 後続フェーズ候補

- 出退勤の月次集計
- 打刻修正申請
- 勤務時間・休憩時間・残業時間の計算
- シフト・所定労働時間管理
- 経費精算
- 領収書ファイル添付
- メール通知
- 有給承認フロー
- 二要素認証の全社員適用
- Web Pushによる退勤忘れ通知
- 通知時刻・通知有無の個人設定
- PC向けElectron版のパッケージングと自動更新
- 代休（休日出勤に応じた休暇）の管理
- 閲覧権限（マネージャー的な仕組み）の管理
- 出退勤データのCSVエクスポート

### 3.3 現時点では対象外

- 給与明細の配信・閲覧
- 給与計算
- 外部勤怠・給与システムとの自動連携
- 過去の有給申請履歴の完全移行
- App Store／Google Playで配布するネイティブスマートフォンアプリ

## 4. 利用者と権限

### 4.1 社員

- 自分の有給残数を閲覧できる
- 自分の有給予定を登録・取消できる
- 自分の有給履歴を閲覧できる
- 自分の代休残数・履歴を閲覧できる
- 自分の勤怠連絡を登録できる
- 自分の出勤・退勤を打刻できる
- 自分のデータを誰に見せるか（閲覧権限）を自分で追加・削除できる
- 他の社員の個人情報、有給残数、履歴は、閲覧権限が付与されていない限り閲覧できない

### 4.2 管理者

- 全社員の基本情報を閲覧・管理できる
- 社員アカウントを作成・無効化できる
  - 無効化は即時反映とし、既存のログインセッション・保持トークン（remember_login_tokens）も同時に失効させる
- 全社員の有給残数、予定、履歴を閲覧できる
- 全社員の代休残数、履歴を閲覧できる
- 有給付与および残数調整を行える
- 社員に代わって有給予定を登録・取消できる
- 全社員の勤怠連絡と出退勤状況を閲覧できる
- 操作履歴と同期結果を確認できる
- 社員アカウントのTOTP（二要素認証）を任意で有効/無効に設定できる
- 閲覧権限（個人単位・グループ単位）を任意の組み合わせで設定できる
- グループ（例：「プログラマー」等）を作成し、所属社員を設定できる
- 指定月の全社員出退勤打刻データをCSVでエクスポートできる

### 4.3 閲覧権限（管理者権限とは独立した仕組み）

管理者権限とは別に、「閲覧のみ」の権限を付与する仕組みを設ける。マネージャーによる部下データの確認や、プロジェクト都合での相互閲覧など、組織上の必要に応じて柔軟に設定できることを目的とする。

- 閲覧権限は個人単位・グループ単位で付与できる
- 権限を持つのは「閲覧」のみとし、登録・取消・調整は一切行えない
- 管理者は任意の組み合わせ（誰が誰を見れるか）を設定できる
- 社員本人は、自分のデータを誰に見せるかを自分で設定できる（セルフサービス）。ただし本人が設定できるのは自分自身を対象（target）とする権限のみで、他人を対象にした設定や、グループ単位の権限設定は管理者のみが行える
- 閲覧権限の付与・削除は操作履歴に記録する

## 5. 認証仕様

- ログインIDは会社のメールアドレスとする
- パスワードは平文保存せず、安全な方式でハッシュ化して保存する
- 管理者が社員アカウントを作成する招待制とする
- 社員自身による自由な新規登録は許可しない
- 初回利用時はメールからパスワードを設定する
- パスワード再設定メールを送信できるようにする
- パスワード再設定URLには有効期限を設ける
- 連続ログイン失敗時は一定時間ログインを制限する
- 一定時間操作がない場合は自動ログアウトする
- 社員は「ログイン状態を保持する」を選択できる
- ログイン保持期間は原則30日とし、管理者は保持対象外または短時間に制限する
- 長期ログインには推測困難なトークンを使用し、トークン本体をデータベースへ保存しない
- 長期ログイン用CookieにはSecure、HttpOnly、SameSite属性を設定する
- 通常のセッションCookieにもSecure、HttpOnly、SameSite（Lax以上）属性を設定する
- 長期ログイントークンは利用時に更新し、再利用を検知できる構成とする
- 長期ログイントークンのローテーション・再利用検知は端末（トークン）ごとに独立して行い、ある端末の更新が他端末のトークンに影響しないようにする
- 通常のログアウトは実行した端末のセッション・トークンのみを失効させる
- パスワード変更、アカウント無効化時は、当該ユーザーの全端末のセッション・長期ログイントークンを一括失効させる
- 管理者画面から特定社員の全端末を強制ログアウトできる操作を用意する（端末紛失時対応）
- ログアウト、パスワード変更、アカウント無効化時は対象トークンを失効させる
- 無効化されたアカウントはログインできない
- 状態変更を伴う全リクエストにCSRFトークンを付与し、サーバー側で検証する
- CSRFトークンはセッションに紐づけ、ログインごとに再発行する
- 打刻・取消・残数調整などの状態変更操作をGETリクエストで実装しない
- 管理者アカウントはTOTP（二要素認証）の有効/無効を設定画面から任意で切り替えられる
- TOTP有効化時はQRコードでシークレットを登録し、バックアップコードを発行する

## 6. 社員向けホーム画面

ホーム画面には以下を表示する。

- 氏名
- 現在の出勤状態
- 出勤・退勤ボタン
- 現在の有給残数
- 登録済みの取得予定日数
- 予定反映後の残有給数
- 代休残数
- 直近の有給予定
- 直近の勤怠連絡
- データ最終更新日時
- 管理担当者への問い合わせ案内

## 7. 有給管理

### 7.1 基本方針

- 有給登録前に、社員がマネージャーまたは上長へ事前確認する
- アプリ内では承認処理を行わない
- 有給登録は「申請」ではなく「事前確認済み予定の登録」として扱う
- 将来必要になった場合に承認フローを追加できるデータ構造とする
- 有給は1日または0.5日単位で管理する
- 半日休暇は午前休と午後休を区別する

登録画面には以下を表示する。

> マネージャーまたは上長への事前確認後に登録してください。この登録による承認処理はありません。

### 7.2 有給残数表示

社員画面では以下を分けて表示する。

- 現在の残有給数
- 登録済みの取得予定日数
- 予定反映後の残有給数

計算式は以下とする。

```text
予定反映後の残有給数
= 現在の残有給数 - 登録済みかつ未取得の予定日数
```

表示例：

```text
現在の残有給数      12.5日
取得予定             1.5日
予定反映後の残数    11.0日
```

### 7.3 有給予定登録

入力項目は以下とする。

| 項目 | 必須 | 内容 |
|---|---:|---|
| 取得日 | 必須 | 有給を取得する日 |
| 取得区分 | 必須 | 1日、午前休、午後休 |
| 消化日数 | 自動 | 1日なら1.0、午前・午後休なら0.5 |
| 連絡事項 | 任意 | 引継ぎや補足情報 |
| 事前確認相手 | 任意 | 確認したマネージャーまたは上長 |

登録時の検証は以下とする。

- 取得区分は定義済みの値だけを許可する
- 消化日数を社員が直接変更できないようにする
- 予定反映後の残数がマイナスになる登録は拒否する
- 同一日に重複した有給を登録できないようにする
- 登録者、登録日時を保存する
- 残数チェックと有給予定登録は同一トランザクション内で行い、複数端末からの同時登録による残数の整合性崩れを防ぐ

### 7.4 有給状態

| 状態 | 内容 |
|---|---|
| 登録済み | 将来または当日に取得予定の有給 |
| 取得済み | 取得日が経過した有給 |
| 取消済み | 予定変更により取り消された有給 |

初期版では承認待ち・承認済み状態は使用しない。ただし将来追加できるよう、状態管理は拡張可能にする。

### 7.5 有給取消

- 本人は登録済みの有給を取り消せる
- 未来日の有給を取り消せる
- 当日の出勤依頼等に対応するため、取得日当日も取り消せる
- 過去の取得済み有給は本人による取消を不可とする
- 過去分は管理者だけが訂正できる
- 取消理由を必須とする
- 取消日時、取消者を保存する
- 取消済みデータは物理削除しない
- 取消前の内容を操作履歴に保存する
- 取消後は取得予定日数から除外し、予定反映後残数を戻す
- 管理者は本人に代わって取消できる

### 7.6 有給付与・失効

有給は付与単位で管理する。

| 項目 | 内容 |
|---|---|
| 付与日 | 有給を付与した日 |
| 付与日数 | 付与した日数 |
| 有効期限 | 付与分が失効する日 |
| 付与理由 | 定期付与、移行残高、管理者調整など |

残数だけでなく、どの付与分がいつ失効するかを保持する。消化時にどの付与分から差し引くかは、原則として有効期限が近いものを優先する。

自動付与処理の詳細は、会社の付与ルールを確認後に確定する。初期運用では管理者による手動付与も可能とする。

### 7.7 管理者による残数調整

- 管理者は残数の増減調整を行える
- 調整理由を必須とする
- 調整前後の値を保存する
- 調整者と調整日時を保存する
- 過去の調整履歴を削除できないようにする

### 7.8 代休

代休は有給とは別枠のデータとして管理する。有給とは発生条件・有効期限のルールが異なるため、テーブル・残数計算を分離する。

**発生ルール**

- §8の勤怠連絡で「休日出勤」が登録されると、同一トランザクション内で代休1日分を自動的に発生させる
- 発生日は休日出勤の対象日とする
- 休日出勤の登録が後から訂正・取消された場合、連動して発生済みの代休も取消す

**有効期限**

- 事業年度単位（4月始まり）で管理し、発生日が属する事業年度の年度末（翌年3月31日）を有効期限とする

**管理・表示**

- 代休の残数・履歴は、有給と同様に「登録済み」「取得済み」「取消済み」の状態で管理する
- 社員ホーム画面（§6）に代休残数を有給とは別に表示する
- 管理者画面（§10）に代休一覧・代休付与状況を表示する

**未確定事項**

- 半日単位の代休（半日出勤に対する半日代休）を扱うかどうかは未確定
- 代休取得時の事前確認フローを有給と同じ厳密さで求めるかは未確定

## 8. 勤怠連絡

現在のGoogleフォームを参考に、以下を登録できるようにする。

- 遅刻
- 早退
- 有給休暇（1日）
- 有給休暇（午前休）
- 有給休暇（午後休）
- 欠勤
- 休日出勤
- 健康診断
- その他

入力項目は以下を基本とする。

- 対象日
- 勤怠種別
- 出勤見込み時刻
- 退勤見込み時刻
- 連絡内容・理由

有給休暇を選んだ場合は、有給予定データとしても登録する。重複登録を避けるため、有給画面と勤怠連絡画面で同じデータを共有する。

休日出勤を選んだ場合は、§7.8の代休発生ルールに従い代休データとしても登録する。

## 9. 出退勤（概略仕様）

出退勤は初期版では基本的な形のみ実装する。勤務時間計算などの詳細は後から確定する。

### 9.1 初期版で行うこと

- ログイン中の本人が出勤ボタンを押せる
- ログイン中の本人が退勤ボタンを押せる
- 打刻日時にはXServer側の日本時間を使用する
- 氏名の選択入力は行わない
- 出勤中か退勤済みかを画面に表示する
- 二重出勤・二重退勤を防止する
- 日付をまたいだ退勤を保存できる
- 社員本人が自分の打刻履歴を確認できる
- 管理者が全社員の打刻履歴を確認できる

### 9.2 後から決めること

- 所定出勤・退勤時刻
- 休憩時間
- 遅刻・早退の自動判定
- 残業時間の計算
- 休日・祝日の扱い
- 打刻忘れ通知
- 打刻修正申請
- 締め日・月次確定
- 給与計算への受渡し
- IPアドレス・位置情報による制限
- 日跨ぎ勤務の業務日判定

### 9.3 退勤忘れ通知

退勤忘れ通知はWeb Pushを使用し、PWAを閉じている状態でも通知を受け取れる構成とする。初期リリース後の後続フェーズで実装する。

- 利用者が画面上の操作により通知を明示的に許可する
- 通知を許可した端末のPush購読情報を社員アカウントと紐付ける
- Cronで定期的に出勤中かつ未退勤の社員を抽出する
- 退勤済み、有給、休日、通知対象時刻前の社員には送信しない
- 同一勤務日に同じ通知を重複送信しない
- 通知をタップすると出退勤画面を直接開く
- 通知時刻は所定退勤時刻または出勤からの経過時間を基準とし、会社ルール確定後に決定する
- 利用者は通知の有効・無効を変更できる

### 9.4 出退勤データのCSVエクスポート

管理者画面から、指定月の全社員分の出退勤打刻データをCSVでエクスポートできる機能を設ける。

**アクセス制御**

- 管理者専用機能とし、§14の所有者照合ミドルウェアで管理者判定を行う
- エクスポート実行を操作履歴（audit_logs）に記録する（実行者、実行日時、対象月）

**出力仕様**

- 対象月を指定して出力する
- 文字コードはBOM付きUTF-8とする（暫定、要確認）
- 初期版の出力項目は社員ID、氏名、日付、出勤時刻、退勤時刻とし、勤怠連絡（遅刻・早退・欠勤等）は含めない（暫定、要確認）
- サーバー上にCSVファイルを永続的に保存せず、生成した内容を直接ストリームでダウンロードさせる。Webルート配下に一時ファイルとして保存しない

## 10. 管理者画面

管理者画面には以下を用意する。

- 社員一覧
- 社員検索
- アカウント作成・無効化
- 社員別有給残数
- 付与分と有効期限の確認
- 有給予定一覧
- 有給取消履歴
- 代休一覧・代休付与状況
- 月間カレンダー
- 有給付与・残数調整
- 勤怠連絡一覧
- 出勤中社員一覧
- 打刻履歴
- 出退勤データのCSVエクスポート（月指定）
- グループ管理（作成・所属設定）
- 閲覧権限の一覧・設定・強制削除
- 操作履歴
- データ移行結果・エラー確認

## 11. データ移行

### 11.1 基本方針

- 運用開始日を境に新アプリへ切り替える
- 過去の有給申請履歴は原則として完全移行しない
- 既存Googleフォームと回答シートは削除せず、閲覧用の過去記録として保存する
- 新アプリ開始後のデータはデータベースを正本とする

### 11.2 有給の初期移行

既存ExcelまたはGoogleスプレッドシートから以下を取り込む。

- 社員情報
- 入社年月
- 有給更新月
- 現在残数
- 次回付与日
- 次回付与日数
- 可能な場合は付与分ごとの失効期限

付与分の内訳を復元できない場合は、運用開始日時点の「移行残高」として登録し、管理者が有効期限を指定する。

### 11.3 出退勤の切替

- 管理者と限定社員で先行テストを行う
- 全社員によるGoogleフォームと新アプリへの二重打刻は行わない
- 全社員の切替日時を明確に決める
- 切替日時まではGoogleフォームを使用する
- 切替日時以降は新アプリを使用する
- Googleフォームは一定期間、障害時の予備として残す

## 12. データ構造（概念）

### 12.1 主なテーブル

| テーブル | 用途 |
|---|---|
| users | ログイン情報、権限、アカウント状態 |
| employees | 氏名、メール、入社日などの社員情報 |
| leave_grants | 有給付与日数、付与日、有効期限 |
| leave_entries | 有給予定、取得、取消状態 |
| leave_adjustments | 管理者による残数調整 |
| comp_leave_grants | 代休の発生記録（発生日、事業年度、有効期限、元になった休日出勤記録への参照） |
| comp_leave_entries | 代休の予定・取得・取消状態 |
| attendance_events | 出勤・退勤の打刻イベント |
| attendance_notices | 遅刻、早退、欠勤等の勤怠連絡 |
| employee_groups | グループ定義（例：「プログラマー」） |
| group_memberships | 社員とグループの所属 |
| view_grants | 閲覧権限（誰が誰／どのグループを閲覧できるか、付与者、期間） |
| audit_logs | 登録、変更、取消等の操作履歴 |
| password_reset_tokens | パスワード再設定用トークン |
| remember_login_tokens | ログイン状態保持用トークン、端末、有効期限 |
| push_subscriptions | Web Pushの端末別購読情報 |
| notification_logs | 退勤忘れ通知等の送信結果と重複防止 |

usersテーブルのアカウント状態は、単純なboolean（有効/無効）ではなく、将来的な区分拡張（休職中等）を見据えたステータス値で管理する。

### 12.2 共通識別子

- 社員は社員IDで一意に識別する
- メールアドレスも重複不可とする
- 氏名は識別子として使用しない
- 各記録には作成日時、更新日時、作成者を保存する

## 13. システム構成

```text
社員・管理者
  ├─ PCブラウザ
  ├─ スマートフォンPWA
  └─ PC向けElectron版
        ↓ 共通のHTTPS通信
XServerレンタルサーバー
        ├─ PHP Webアプリ
        ├─ 認証・権限制御
        ├─ Web Push送信
        ├─ 有給・勤怠機能
        └─ MySQL/MariaDB
```

### 13.1 想定環境

- XServerレンタルサーバー
- PHP
- MySQLまたはMariaDB
- 独自サブドメイン
- 無料独自SSL
- Cronによる定期処理
- SMTPまたはXServerメールによるパスワード再設定メール送信

URL例：

```text
https://attendance.example.co.jp
```

### 13.2 PWA構成

- Web App Manifestを用意する
- iPhone・Android用のアプリアイコンを用意する
- `display: standalone`によりブラウザのアドレスバーを表示せず起動する
- Service Workerを登録し、画面資産の更新とWeb Push受信を扱う
- すべてのPWA機能をHTTPS上で提供する
- オフライン時の打刻は原則不可とし、通信できないことを明示する
- アプリ更新はサーバー反映を基本とし、利用者による再インストールを不要とする

### 13.3 Electron構成

- Electron版はPC向けの任意クライアントとし、PWA／ブラウザ版と同じWebアプリを表示する
- Windowsを優先し、macOS対応は利用状況に応じて判断する
- 業務ロジックや勤怠データをElectron内へ重複実装・保存しない
- Electron固有機能はデスクトップ通知、専用ウィンドウ、自動起動等に限定する
- 配布方法、コード署名、自動更新方式は導入前に確定する
- Electron版が利用できない場合もブラウザ版で全業務を継続できるようにする

## 14. セキュリティ要件

- すべての通信をHTTPS化する
- HTTPアクセスはHTTPSへ転送する
- 社外のスマートフォン・PCからアクセス可能とする
- 権限判定は画面表示だけでなくサーバー側で行う
- URLやパラメーターを変更しても他社員のデータを取得できないようにする
- 本人データを扱う全APIは、リクエストされたリソースIDの所有者（またはアクセス権限）をサーバー側で必ず照合してから応答する。ログイン中かどうかのチェックだけでなく、そのリソースが自分のものか、または有効な閲覧権限（view_grants）があるかまで確認する
- 上記の所有者・閲覧権限照合ロジックは共通関数・共通ミドルウェアとして1箇所に集約し、エンドポイントごとに個別実装しない
- 閲覧権限（view_grants）経由のアクセスは、読み取り操作のみ許可し、登録・取消・調整は拒否する
- パスワードを平文で保存しない
- SQLインジェクション、XSS対策を行う
- CSRF対策として、状態変更を伴う全リクエストにCSRFトークンを付与しサーバー側で検証する
- ログイン試行回数を制限する
- セッションCookieに安全な属性を設定する
- 長期ログイントークンとPush購読情報を機密情報として扱う
- 通知本文には有給残数や詳細な勤怠理由等の機微情報を表示しない
- ElectronではNode.js integrationを無効化し、context isolationを有効化する
- Electronから任意の外部ページを同一画面内で開かない
- Google連携等の認証情報を公開ディレクトリへ置かない
- XServerのWAFを有効にする
- 本番環境で詳細なエラー情報を画面表示しない
- 退職者・休職者のアカウントを無効化できるようにする
- アカウント無効化時は以下を同時に実行する
  - 当該ユーザーの全remember_login_tokensを失効させる
  - 実行中の全セッションを即座に無効化する
  - 未使用のpassword_reset_tokensを失効させる
  - push_subscriptionsを削除する
  - 当該ユーザーが関わるview_grants（自分が対象／自分が付与したもの）の扱いを整理する
- 出退勤CSVエクスポートは管理者専用とし、実行を操作履歴に記録する。サーバー上に一時ファイルを永続保存しない
- データベースとアプリケーションファイルを定期バックアップする

## 15. 操作履歴

以下の操作を履歴として保存する。

- ログイン成功・失敗
- ログアウト
- 有給予定登録
- 有給取消
- 代休の発生・取消
- 管理者による代理登録・代理取消
- 有給付与
- 残数調整
- 出勤・退勤打刻
- 管理者によるデータ修正
- アカウント作成・無効化
- 閲覧権限の付与・削除
- 出退勤CSVエクスポートの実行

履歴には最低限以下を保存する。

- 操作者
- 操作日時
- 操作種別
- 対象データ
- 変更前の値
- 変更後の値

接続元IPアドレスは取得・保存しない。

## 16. 非機能要件

- PCとスマートフォンの主要ブラウザで利用できる
- iPhone・AndroidでPWAとしてホーム画面へ追加し、単独のアプリ画面として起動できる
- PWA未対応または通知を許可しない端末でも、ブラウザ版の基本機能を利用できる
- Electron版はWeb版と同じサーバー機能を利用し、Web版との機能差を最小限にする
- 社員約20名の利用を想定する
- 通常画面は実用上支障のない速度で表示する
- 日時は日本標準時で統一する
- データベースには日時を一貫した形式で保存する
- 障害時に管理者が既存Googleフォームへ一時的に戻せるようにする
- バックアップから復旧できる手順を用意する
- 本番環境と開発・検証環境を可能な範囲で分離する

## 17. MVP受入条件

以下を満たした場合、MVPとして受入可能とする。

- 有効な社員アカウントでログインできる
- 無効化された社員はログインできない
- 社員が他社員の有給情報を閲覧できない（閲覧権限が付与されている場合を除く）
- 社員が自分の残有給数を確認できる
- 1日、午前休、午後休を登録できる
- 半日登録時に0.5日として反映される
- 残数を超える有給登録が拒否される
- 登録済み有給を本人が取り消せる
- 取消後に予定反映後残数が戻る
- 取消済みデータと操作履歴が残る
- 管理者が全社員の有給状況を確認できる
- 管理者が有給を付与・調整できる
- 社員が出勤・退勤を基本操作として記録できる
- 二重出勤・二重退勤が防止される
- 社外のスマートフォンからHTTPSで利用できる
- パスワード再設定メールが動作する
- 社員がログイン状態を保持するか選択でき、保持トークンを安全に失効できる
- PWAをホーム画面へ追加し、アプリ形式で起動できる
- ログイン中のユーザーが、URLやリクエストパラメーターのIDを他社員のものに変更しても、閲覧権限のない限り閲覧・登録・取消・打刻のいずれも実行できないこと（社員・管理者それぞれで確認）
- 状態変更を伴うAPIへCSRFトークンなしでリクエストした場合、拒否されること
- アカウント無効化操作を行うと、対象ユーザーの既存セッション・長期ログイントークンが即座に失効し、以後のAPIリクエストが全て拒否されること

## 18. 未確定事項

以下は実装前または後続フェーズで決定する。

- 管理者の人数と対象者
- 社員番号の採番・既存番号の有無
- 会社の正式な有給付与・繰越・失効ルール
- 移行残高の失効期限の決め方
- 当日取消を何時まで許可するか
- 登録・取消時のメール通知要否
- 有給予定カレンダーを社員同士で共有するか
- 管理者が社員の連絡事項・取消理由を閲覧できる範囲
- 出退勤の詳細ルール
- 経費精算の承認経路
- ログとバックアップの保存期間
- 一般社員と管理者のログイン保持期間
- 退勤忘れ通知の基準時刻、再通知回数、対象曜日
- 通知を必須運用とするか任意設定とするか
- Electron版の対象OS、社内配布方法、コード署名、自動更新方式
- TOTPリカバリーコード再発行のフロー（誰が承認するか）
- 休職中ステータスの詳細な扱い（復職時のデータ継続方法等）
- 半日単位の代休を扱うかどうか
- 代休取得時の事前確認フローの厳密さ
- 出退勤CSVの文字コード（暫定：BOM付きUTF-8）
- 出退勤CSVに勤怠連絡を含めるか（暫定：含めない）
- 退職者アカウント無効化時のview_grantsの扱い（削除するか、履歴として残すか）

## 19. 開発優先順位

1. ログイン・社員管理・権限制御
2. 有給残数・付与・予定登録・取消
3. 管理者向け有給一覧・操作履歴
4. 基本的な出勤・退勤打刻
5. 遅刻・早退・欠勤等の勤怠連絡
6. PWA対応・ログイン保持
7. Web Pushによる退勤忘れ通知
8. 出退勤詳細・CSV出力
9. PC向けElectron版
10. 経費精算
11. 代休管理
12. 閲覧権限（グループ・個人単位の閲覧共有）
